apiVersion: v1
kind: Service
metadata:
  name: {{ include "D1DBSample.fullname" . }}
  labels:
    {{- include "D1DBSample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "D1DBSample.selectorLabels" . | nindent 4 }}
