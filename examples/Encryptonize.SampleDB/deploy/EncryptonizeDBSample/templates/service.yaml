apiVersion: v1
kind: Service
metadata:
  name: {{ include "EncryptonizeDBSample.fullname" . }}
  labels:
    {{- include "EncryptonizeDBSample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "EncryptonizeDBSample.selectorLabels" . | nindent 4 }}
